-- 코드를 입력하세요
SELECT product_id, product_name, product_cd, category, price from (select rank() over(order by price desc) as rank, product_id, product_name, product_cd, category, price from food_product) where rank = 1;